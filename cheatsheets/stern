# https://github.com/stern/stern
# Multi pod and container log tailing for Kubernetes

stern . -n $namespace
stern . -n $namespace -c container-name

# All logs for pod matching "appname" in namespace and context
stern appname -n $namespace --context $context
