# lsof package e.g. yum install lsof

# Check listening ports
sudo netstat -tunlp  # see netstat's cheats for more details

# See what port a given process ID has open
lsof -p PID

# Check which process is using a given port
lsof -i :PORT_NUMBER
