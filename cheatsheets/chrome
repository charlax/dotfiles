# Copy all URLs in Network DevTools tab
# 1. Detach the DevTools
# 2. Press Alt+Shift+I to open DevTools in DevTools
# 3. Run this in console:
copy(UI.panels.network.networkLogView.dataGrid.rootNode().flatNodes.map(n => n.request().url()).join('\n'))

# Search in tabs
cmd+shift+a
