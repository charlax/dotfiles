;; vi:syntax=clojure
;; https://clojure.org/api/cheatsheet

;; Maps
(def my-map {:first-name "Chew" :last-name "Bacca"})

;; Strings

;; Concat
(str "a" "b")
;; Join
(clojure.string/join ", " [1 2 3])
