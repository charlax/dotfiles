$()
// equivalent to
document.querySelector()

$$()
// equivalent to
document.querySelectorAll()
copy([...$$(".hdlist1")].map(e => e.innerText))

$0, $1, $2, ... $n
// references to the elements you have recently selected in the Elements panel.
// $0 represents the most recently selected element, $1 represents the second...

$x()
// allows you to query the DOM using XPath expressions
$x("//div") // would return all div elements on the page.

// https://alan.norbauer.com/articles/browser-debugging-tricks

// See also:
// ./javascript

// vim: set ft=javascript:
