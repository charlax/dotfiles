# https://docs.rs/regex/1.3.9/regex/#syntax

# search in a folder
rg stringtomatch ./folder

# include hidden files (which are excluded by default)
rg --hidden $string
# include more files with -u -uu -uuu
rg -uuu $string

# Show file names only
rg -l $string
