# Show current and latest version for all packages
poetry show -l
poetry show -lo  # only for outdated packages

# Add dev dependency
poetry add -D $dependency

# Delete the virtualenv
rm -rf `poetry env info -p`

# Show env
poetry env info -p
