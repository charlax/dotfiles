# true if file exists and has size greater than zero.
[[ -s /toaster.conf ]]

# true if variable is *not* empty
[[ -s "" ]]

# and
[[ -s "" ]] && [[ ... ]]

# Tests

# -z "$string"          True if length of string is *z*ero
