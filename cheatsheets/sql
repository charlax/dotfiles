# SQL

See Cookbook

## Datetime

Group by hour and minute:

```sql
select date_format(created_at, "%Y-%m-%d %H:%i")
from table
group 1
order by 1 asc
```

## Postgres

```sql
-- datetime
now() - interval '2 days'
```
