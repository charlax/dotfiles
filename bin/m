#!/usr/local/bin/zsh

if [[ "$1" == "" ]]
then
    $EDITOR_GUI .
elif [[ $1 =~ "https://github.com/.*" ]]
then
    # See ZSH Parameter (Variable) Expansion
    repo_dir=${${1##*/}%.*}
    git clone $1 && cd $repo_dir && $EDITOR_GUI .
else
    $EDITOR_GUI "$1"
fi
