#!/bin/bash

. "$(dirname "$0")/../helpers/setup.sh"

printf ""
printf "${GREEN}Updating $DOTFILES ${NC}"

git -C $DOTFILES pull
python3 $DOTFILES/install.py

printf ""
printf "${GREEN}Update Vim dotfiles $VIM_DOTFILES ${NC}"

git -C $VIM_DOTFILES pull
