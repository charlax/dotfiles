#!/bin/bash

. "$(dirname "$0")/../helpers/setup.sh"

printf "\n${GREEN}Updating $DOTFILES ${NC}\n"

git -C $DOTFILES pull
python3 $DOTFILES/install.py

printf "\n${GREEN}Update Vim dotfiles $VIM_DOTFILES ${NC}\n"

git -C $VIM_DOTFILES pull
