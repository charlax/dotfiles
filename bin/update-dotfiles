#!/bin/bash

# shellcheck source=../helpers/setup.sh
. "$(dirname "$0")/../helpers/setup.sh"

log_info "Updating $DOTFILES"

git -C "$DOTFILES" pull
python3 "$DOTFILES/install.py"

log_info "Update Vim dotfiles $VIM_DOTFILES"

if [[ -f "$VIM_DOTFILES" ]]; then
  git -C "$VIM_DOTFILES" pull
fi
