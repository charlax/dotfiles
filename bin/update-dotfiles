#!/bin/bash

. "$(dirname "$0")/../helpers/setup.sh"

echo ""
echo "${GREEN}Updating $DOTFILES ${NC}"

git -C $DOTFILES pull
python $DOTFILES/install.py

echo ""
echo "${GREEN}Update Vim dotfiles $VIM_DOTFILES ${NC}"

git -C $VIM_DOTFILES pull
