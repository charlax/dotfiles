# Sample gitconfig
#
# This is a sample gitconfig. Usually we keep a lot of sensitive information
# here (things like authentication tokens, email addresses, and so on), so this
# file is ignored in this project's .gitignore.
#
# To set this up, rename this file to `gitconfig`
[user]
    name = Charles-Axel Dein
    email = yourname@example.com
[color]
    ui = true

[alias]
    unstage = reset HEAD
    uncommit = reset --soft HEAD^
    co = checkout
    l = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    changes = log --pretty=format:\"%h %cr %cn %Cgreen%s%Creset\" --name-status
    short = log --pretty=format:\"%h %cr %cn %Cgreen%s%Creset\"
    changelog = log --pretty=format:\" * %s\"
    c = !git add . && git commit -a && git push
    recommit = !git add . && git commit --amend
    go = checkout -B
    d = diff
    s = status
    st = status
    stat = status
    b = branch -v

[core]
    excludesfile = ~/.gitignore
    quotepath = false
    editor = vim

[merge "pofile"]
    name = Gettext merge driver
    driver = git merge-po %O %A %B

[url "git@github.com:/"]
    insteadOf = "gh:/"

[push]
    default = upstream
