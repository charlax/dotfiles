# Contributing

## Pre-commit hooks

This repo is using [prek](https://github.com/j178/prek), a faster drop-in alternative to pre-commit. Hooks are defined in `.pre-commit-config.yaml`.

```bash
# updating plugins
$ prek autoupdate

# do a simple run
$ prek run
```
